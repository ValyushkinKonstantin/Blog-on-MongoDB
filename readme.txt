1) You have to run mongose use terminal (sudo mongod)(don't close this tab terminal);
2) Open a new windows terminal and run mongo
3) Open a new tab in terminal and run node.js server (nodeman app.js);
4) If you see that connecting is successfully try to add information  to db
5) use command "show dbs" in mongo terminal to find information manualy.
 
